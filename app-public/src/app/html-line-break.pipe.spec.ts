import { HtmlLineBreakPipe } from './html-line-break.pipe';

describe('HtmlLineBreakPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlLineBreakPipe();
    expect(pipe).toBeTruthy();
  });
});
